## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` was run and `dist/` is committed if `src/` changed
- [ ] README/docs updated if behavior or inputs/outputs changed
